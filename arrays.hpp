#ifndef ARRAYS
#define ARRAYS
#include <vector>
#include <string>
#include "location.hpp"
#include "story.hpp"

std::string prefix[1];
std::string interfix[1];
std::string suffix[1];
std::vector<story> stories[1];
std::vector<location> locations;
#endif